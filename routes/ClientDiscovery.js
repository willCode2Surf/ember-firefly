var crypto = require('crypto');
var riak = require('nodiak').getClient('http', 'RIAKCLUSTER', RIAKCLUSTERPORT);
var http = require("http");

var ClientDiscovery = function(req, res)
{
	
};

ClientDiscovery.prototype.Discover = function()
{
    
};

module.exports= ClientDiscovery;
