
var riak = require('nodiak').getClient('http', 'RIAKCLUSTER', RIAKCLUSTERPORT);
var http = require("http");

var RestData = function(req, res)
{
	
};

RestData.prototype.ProcessRequest = function()
{
    var self = this;
	
};

module.exports= RestData;
